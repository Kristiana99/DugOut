spring.application.name=DugOut

server.servlet.context-path=/  

server.port = 8080

spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

server.servlet.jsp.init-parameters.development = true

mybatis.mapper-locations=classpath:mapper/**/*Mapper.xml

# datasource 설정
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.url=jdbc:oracle:thin:@localhost:1521/XEPDB1
spring.datasource.username=baseball
spring.datasource.password=1234

mybatis.configuration.log-impl=org.apache.ibatis.logging.slf4j.Slf4jImpl

# SQL 실행 시 파라미터 로그를 출력하기 위한 설정
logging.level.org.apache.ibatis=DEBUG

## 파일업로드를 위해 추가된 부분
# file upload setting - no need library
spring.servlet.multipart.enabled=true
# 파일 저장 경로
spring.servlet.multipart.location=C:/dev/data/
# 업로드 될 파일당 최대 크기
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=1000MB
# properties 변수 
part1.upload-path=C:/dev/springboot/data/



